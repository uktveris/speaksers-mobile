import { useEffect } from "react";

interface usePeerConnProps {
  remoteSocketId: string;
  initCall: boolean;
}

function usePeerConn({ remoteSocketId, initCall }: usePeerConnProps) {
  useEffect(() => {}, []);
}
